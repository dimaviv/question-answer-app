import React from 'react';

const Profile = () => {
    return (
        <div>
            It's your profile, change it to have more attractive profile!
        </div>
    );
};

export default Profile;