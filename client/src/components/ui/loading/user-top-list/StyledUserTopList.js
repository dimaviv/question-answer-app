import styled from 'styled-components';

export const StyledUserTopListLoading = styled.div`
  width: 420px;
  height: 620px;
  background-color: ${props => props.theme.accentColor};
  border-radius: 15px;
`